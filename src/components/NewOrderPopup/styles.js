import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    root: {
        position: "absolute",
        bottom: 500,
    },
    userBG: {
        margin: 5,
        borderWidth: 4,
        borderColor: "white",
        borderRadius: 50,
    },
    something: {
        bottom: 550,
        right: -39,
    }
});

export default styles;
